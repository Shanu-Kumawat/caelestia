general {
    ignore_dbus_inhibit = false
}

listener {
    timeout = 860  # 14 minutes - notify 1 min before suspend
    on-timeout = notify-send "Idle Warning" "System will suspend in 1 minute" -u critical
}

listener {
    timeout = 900  # 15 minutes - suspend
    on-timeout = systemctl suspend
}
